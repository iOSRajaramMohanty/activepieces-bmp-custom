/**
 * EE Embed SDK Stub
 * 
 * This is a stub module for the Enterprise Edition embed SDK.
 * In the Community Edition SDK, all EE features are excluded.
 * Components that conditionally use EE features should check for their existence.
 */

export const EmbeddingProvider = null;
export const EmbeddingContext = null;

// Stub for ActivepiecesClientEventName enum
export enum ActivepiecesClientEventName {
  CLIENT_BUILDER_HOME_BUTTON_CLICKED = 'CLIENT_BUILDER_HOME_BUTTON_CLICKED',
}

// Export empty object to prevent import errors
export default {};
